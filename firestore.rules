rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
  
    // WARNING: Insecure rules for prototype only.
    // This allows all reads and writes to the entire database.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
