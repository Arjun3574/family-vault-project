rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow all reads and writes for authenticated users for testing
      allow read, write: if request.auth != null;
    }
  }
}
